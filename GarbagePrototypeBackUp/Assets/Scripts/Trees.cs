using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Trees : MonoBehaviour {

    // Use this for initialization
    void Start()
    {
        //LeanTween.rotate(gameObject, new Vector3(gameObject.transform.localRotation.x + 1f, gameObject.transform.localRotation.y + 1f, gameObject.transform.localRotation.z + 1f), .5f).setLoopPingPong();
    }
	// Update is called once per frame
	void Update () {
		
	}
}
